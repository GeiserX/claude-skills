# Contracts — the frozen surfaces this repo exposes

> Optional. The stable API/ABI/CLI that independently-built components and consuming repos code against.
> Anything listed here is a promise: change it only by announcing on `COORDINATE.md` **before** merge.

## 1. {{FIRST_SURFACE}}

_(struct / signature / wire format / CLI — byte-accurate where it matters)_

## Frozen invariants

- {{FIRST_INVARIANT}}
